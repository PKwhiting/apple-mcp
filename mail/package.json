{
  "name": "@griches/apple-mail-mcp",
  "version": "1.0.0",
  "description": "MCP server for interacting with Apple Mail on macOS",
  "main": "build/mail/src/index.js",
  "bin": {
    "apple-mail-mcp": "build/mail/src/index.js"
  },
  "files": [
    "build/**/*.js",
    "build/**/*.d.ts",
    "README.md"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('build', { recursive: true, force: true })\" && tsc",
    "start": "node build/mail/src/index.js",
    "test": "npm run build && node --experimental-strip-types --no-warnings --test tests/safety.test.ts tests/privacy.test.ts",
    "test:clean": "npm run build && node --experimental-strip-types --no-warnings --test tests/safety.test.ts tests/privacy.test.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "apple",
    "mail",
    "email",
    "macos",
    "applescript"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/griches/apple-mcp.git",
    "directory": "mail"
  },
  "homepage": "https://github.com/griches/apple-mcp/tree/main/mail",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "openredaction": "^1.1.2",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
